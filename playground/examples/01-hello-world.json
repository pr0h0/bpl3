{
  "order": 1,
  "title": "Hello World",
  "snippet": "Your first BPL program",
  "description": "The classic first program. Every BPL program starts with a main frame that returns an integer. This example imports printf from the lightweight standard C declarations so the first playground run stays fast.",
  "code": [
    "import [printf] from \"std/c.bpl\";",
    "",
    "frame main() ret int {",
    "    # Print a message to stdout",
    "    printf(\"Hello, World!\\n\");",
    "    ",
    "    # Return 0 to indicate success",
    "    return 0;",
    "}"
  ]
}
