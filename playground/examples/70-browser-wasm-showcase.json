{
  "order": 70,
  "title": "Browser WebAssembly Showcase",
  "snippet": "Compile hosted wasm and execute the module inside the browser.",
  "description": "This example stays native-compatible while also using the hosted WebAssembly runtime. In the playground, Run Code executes it as a native binary and Run Wasm compiles it to wasm32, instantiates it in the browser, and routes stdout through JavaScript host imports.",
  "wasm": {
    "mode": "wasm-hosted",
    "browserRuntime": true,
    "canonicalMatrixFile": "examples/wasm_hosted_transform/main.bpl",
    "expectedReturn": 0,
    "expectedStdout": "BPL hosted wasm demo\nargv[1]: browser\nfib(7) verified\n",
    "expectedStderr": "",
    "reason": "Hosted browser wasm showcase that uses argv, stdout, stdlib String, and pure control flow through the JavaScript host adapter."
  },
  "code": [
    "import [printf] from \"std/c.bpl\";",
    "import [puts] from \"std/c.bpl\";",
    "extern __bpl_argc() ret int;",
    "extern __bpl_argv_get(index: int) ret string;",
    "",
    "frame fib(n: int) ret int {",
    "    if (n < 2) {",
    "        return n;",
    "    }",
    "    return fib(n - 1) + fib(n - 2);",
    "}",
    "",
    "frame main() ret int {",
    "    printf(\"BPL hosted wasm demo\\n\");",
    "    printf(\"argv[1]: \");",
    "    if (__bpl_argc() > 1) {",
    "        puts(__bpl_argv_get(1));",
    "    } else {",
    "        puts(\"none\");",
    "    }",
    "    if (fib(7) != 13) {",
    "        return 13;",
    "    }",
    "    printf(\"fib(7) verified\\n\");",
    "    return 0;",
    "}"
  ],
  "input": "",
  "args": ["browser", "wasm"],
  "expectedOutput": "BPL hosted wasm demo\nargv[1]: browser\nfib(7) verified\n"
}
