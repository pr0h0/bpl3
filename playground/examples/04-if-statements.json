{
  "order": 4,
  "title": "If Statements",
  "snippet": "Conditional execution",
  "description": "If statements control program flow based on boolean conditions. BPL requires explicit boolean conditions. Braces {} are mandatory even for single statements. You can chain multiple conditions using 'else if'. This example demonstrates basic if/else, else-if chains, and comparison operators.",
  "code": [
    "extern printf(fmt: string, ...) ret int;",
    "",
    "frame main() ret int {",
    "    local age: int = 18;",
    "    ",
    "    # Simple if statement",
    "    if (age >= 18) {",
    "        printf(\"You are an adult\\n\");",
    "    }",
    "    ",
    "    # If-else statement",
    "    local score: int = 85;",
    "    ",
    "    if (score >= 90) {",
    "        printf(\"Grade: A\\n\");",
    "    } else if (score >= 80) {",
    "        printf(\"Grade: B\\n\");",
    "    } else if (score >= 70) {",
    "        printf(\"Grade: C\\n\");",
    "    } else {",
    "        printf(\"Grade: F\\n\");",
    "    }",
    "    ",
    "    # Comparison operators",
    "    local x: int = 5;",
    "    local y: int = 10;",
    "    ",
    "    if (x == y) { printf(\"Equal\\n\"); }",
    "    if (x != y) { printf(\"Not equal\\n\"); }",
    "    if (x < y) { printf(\"%d < %d\\n\", x, y); }",
    "    ",
    "    return 0;",
    "}"
  ]
}
