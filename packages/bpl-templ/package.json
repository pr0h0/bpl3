{
  "name": "bpl-templ-cli",
  "version": "0.1.0",
  "description": "CLI for compiling BPL templates",
  "scripts": {
    "build": "bpl build ./src/cli.bpl --compile --outfile ./dist/bpl-templ"
  },
  "dependencies": {
    "commander": "^11.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0"
  }
}
